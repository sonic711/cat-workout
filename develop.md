**專案目標：**
開發一個基於 Vue 3 的「健身日誌」單頁應用程式 (SPA)，讓使用者能夠記錄、查詢並視覺化自己的訓練歷史。

**技術棧：**

* **前端框架：** Vue 3
* **UI 元件庫：** Element Plus
* **狀態管理：** Pinia
* **路由管理：** Vue Router
* **數據儲存：** 採用無需自行開發後端的方式實現數據的永久儲存。應用程式需整合 **SQLite**，以確保訓練紀錄不會因更換裝置或清除快取而遺失。

**核心功能需求：**

1. **月曆主畫面：**
    * 應用程式首頁為一個月曆檢視。
    * 若某個日期有安排或記錄了訓練，則在該日期下方顯示對應的「身體部位標籤」(例如：腿、肩)。
    * 這些標籤是根據當天記錄的所有動作所屬的身體部位自動生成的。
2. **訓練日誌詳情頁：**
    * 使用者點擊月曆上的任一日期後，會進入該日期的訓練日誌頁面。
    * 在此頁面中，使用者可以自由地新增、修改或刪除當日的訓練內容。此操作支援對過去、當天或未來日期的規劃與記錄。
3. **動作管理系統：**
    * 使用者在日誌中新增訓練動作時，可以從一個「已建立動作列表」中選取，或者動態建立一個全新的動作。
    * **建立新動作時**，使用者必須定義兩項資訊：
        * **動作名稱** (例如：槓鈴深蹲)。
        * **對應的身體部位** (例如：腿)。這個部位將成為月曆上顯示標籤的來源。
    * 所有使用者建立的動作都應被永久儲存，並可在未來重複選用。
4. **詳細的組數記錄：**
    * 針對每一個訓練動作，使用者需要能夠**逐組記錄**其詳細表現。
    * 每一組的記錄都必須包含以下三個欄位：
        * **重量** (數值)。
        * **重量單位** (使用者在該次輸入時，可自行選擇**公斤(kg)** 或 **磅(lb)**)。
        * **次數** (該組完成的重複次數)。
    * 介面應提供清晰的方式讓使用者能為一個動作動態新增多筆組數記錄。

**目前開發狀態：**

* 專案已脫離 Vite 預設樣板，首頁改為 Element Plus 月曆佈局雛形。
* 已整合 Element Plus 並在全域匯入樣式，後續元件可直接使用 Element Plus 元件。
* 首頁已改為 Element Plus 月曆佈局雛形，包含左右兩欄：月曆檢視與選定日期詳情占位內容。
* 尚未整合 Pinia 自訂 stores 或 SQLite 永久儲存邏輯。

**下一步 TODO：**

* 釐清首頁資料流與日誌詳情元件設計，準備串接 Pinia 與 SQLite。
* 制定初始狀態資料結構（Pinia store 與 SQLite schema）以支援訓練紀錄保存。
* 規劃日誌詳情區域的實際內容（動作列表、組數與標籤顯示）並逐步實作。
