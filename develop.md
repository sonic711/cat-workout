**專案目標：**
開發一個基於 Vue 3 的「健身日誌」單頁應用程式 (SPA)，讓使用者能夠記錄、查詢並視覺化自己的訓練歷史。

**技術棧：**

* **前端框架：** Vue 3
* **UI 元件庫：** Element Plus
* **狀態管理：** Pinia
* **路由管理：** Vue Router
* **數據儲存：** 透過 Node/Express + MySQL 永久儲存訓練資料；後端提供 REST API，前端以 fetch 串接，確保換裝置或清除快取時資料仍在。

**核心功能需求：**

1. **月曆主畫面：**
    * 應用程式首頁為一個月曆檢視。
    * 若某個日期有安排或記錄了訓練，則在該日期下方顯示對應的「身體部位標籤」(例如：腿、肩)。
    * 這些標籤是根據當天記錄的所有動作所屬的身體部位自動生成的。
    * 點擊任一日期後，介面需自動捲動或聚焦到訓練詳情面板，確保使用者立即看到內容。
2. **訓練日誌詳情頁：**
    * 使用者點擊月曆上的任一日期後，會進入該日期的訓練日誌頁面。
    * 在此頁面中，使用者可以自由地新增、修改或刪除當日的訓練內容。此操作支援對過去、當天或未來日期的規劃與記錄。
3. **動作管理系統：**
    * 使用者在日誌中新增訓練動作時，可以從一個「已建立動作列表」中選取，或者動態建立一個全新的動作。
    * **建立新動作時**，使用者必須定義：
        * **動作名稱** (例如：槓鈴深蹲)。
        * **訓練分類**（**重量訓練** 或 **有氧運動**）。
        * 若為重量訓練，還需填寫 **對應的身體部位** (例如：腿)；有氧運動則僅需時間紀錄，不需部位。
    * 月曆會依據重量訓練的部位或有氧運動標籤顯示每日摘要。
    * 所有註冊會員共用同一份動作庫：新建動作會立即對所有會員開放選用，系統需以動作名稱 + 分類維持唯一性，避免不同會員重複建立相同動作。
    * 後端儲存動作時不再綁定會員識別，而是以全域動作表管理；既有紀錄若引用已刪除的動作，需導向預設備援或禁止刪除。
4. **詳細的組數記錄：**
    * 針對每一個訓練動作，使用者需要能夠**逐組記錄**其詳細表現。
    * 每一組的記錄都必須包含以下三個欄位：
        * **重量** (數值)。
        * **重量單位** (使用者在該次輸入時，可自行選擇**公斤(kg)** 或 **磅(lb)**)。
        * **次數** (該組完成的重複次數)。
    * 介面應提供清晰的方式讓使用者能為一個動作動態新增多筆組數記錄。
5. **飲食與水分紀錄：**
    * 每日可針對早餐 / 午餐 / 晚餐輸入多筆飲食項目，紀錄名稱、熱量與備註。
    * 支援輸入當日喝水量（ml），並與飲食項目一併保存於指定日期的訓練紀錄中。
    * 首頁詳情面板需回顯「每日飲食」概要，列出各餐熱量與總攝取量。
6. **教練課標示：**
    * 訓練紀錄可切換「教練課」旗標，方便識別需要額外注意的日程。
    * 月曆對應日期需加上黃色框線與背景提示，詳情面板也要顯示「教練課」標籤。
7. **每日體重紀錄：**
    * 使用者可為每一天輸入體重數值。
    * 體重以公斤 (kg) 為單位儲存與顯示，便於追蹤趨勢。

**目前開發狀態：**

* 專案已脫離 Vite 預設樣板，首頁使用 Element Plus 建立月曆 + 詳情雙欄版面。
* 已整合 Element Plus 並在全域匯入樣式，後續元件可直接使用 Element Plus 元件。
* 優化首頁版面視覺：置中主內容、調整卡片圓角陰影與字體層級，並針對 1200px 以下與手機尺寸加入 RWD。
* 月曆日期格顯示 `useWorkoutStore` 內的身體部位標籤，並以雙欄晶片排版呈現，確保多個部位時仍能一目了然；暫以示範資料 hydrate（後續會替換為實際儲存機制）。
* 詳情面板可呈現選定日期的訓練內容、組數表格與部位標籤，資料來源為 Pinia store。
* 新增 `useWorkoutStore`（Pinia）與 `@/types/workout`，定義訓練紀錄資料結構與 SQLite 串接所需的初始方法。
* 建立 `PersistenceService` 介面與 `createDemoPersistenceService`，再透過 `persistenceProvider` 管理服務實例。
* Pinia `useWorkoutStore` 改為直接呼叫 `PersistenceService` 進行 hydrate / save / clear，頁面僅需觸發 store 的 `hydrateFromPersistence`。
* 以 Express + MySQL 建立 REST API（`server/index.ts`），將資料儲存在資料庫中，支援 load/save/clear 並以 `tenant_id` 分流使用者資料。
* 新增 `createMysqlPersistenceService`，以 fetch 串接 `/api/*` 端點並依登入帳號帶上 `x-storage-key` header。
* `persistenceProvider` 預設注入 MySQL persistence（失敗時退回 demo service）。
* 新增 `WorkoutSessionEditor` 對話框，支援建立 / 編輯 / 刪除指定日期的訓練紀錄，含項目、組數與 inline 新增動作。
* `WorkoutSessionEditor` 新增組數時會自動沿用上一組的重量 / 單位 / 次數 / 備註，且新建組數預設使用磅（lb）單位，縮短重複輸入時間。
* 引入 `useResponsiveDialog` hook，讓訓練紀錄編輯與動作管理彈窗在手機端自動調整寬度、頂距與排版，提升行動裝置的可用性。
* 首頁詳情面板加入「管理訓練紀錄」入口，無紀錄時可直接建立新資料。
* 建置 `authStore` 與登入面板，支援帳號 / 密碼驗證；僅輸入帳號為唯讀模式，帳號密碼皆正確（admin/admin、sean/sean）可進入對應帳號的編輯模式。
* 登入後會將使用者與模式持久化於瀏覽器儲存，重新整理後仍保留登入狀態，直到手動登出或清除 cookies。
* 登入成功後，頁首登入區塊會收合為精簡狀態，釋出更多畫面空間給主要內容。
* 按使用者帳號分流 SQLite 儲存檔案，登入時自動切換到個人專屬資料庫並載入訓練紀錄。
* 編輯操作受權限控管，唯讀模式下 UI 按鈕停用並顯示提示訊息，確保日誌僅能由授權者修改。
* `ExerciseManager` 對話框提供訓練動作列表、使用次數與建立 / 更新時間，支援新增、編輯、刪除（有紀錄引用時禁止刪除）。現已支援「重量訓練 / 有氧運動」分類，有氧動作不需填寫部位。
* `useWorkoutStore` 新增動作使用統計、更新與刪除 API，並確保名稱唯一；首頁登入面板加入「管理 / 檢視訓練動作」入口。Store、日曆摘要與 MySQL schema 已支援有氧動作的時間紀錄與分類標籤。
* `WorkoutSessionEditor` 新增「每日飲食」分區，可輸入各餐飲食項目、熱量與喝水量，並將資料序列化後交由 store / 後端保存；重量與次數欄位改為下拉選單，方便快速選取常用數值。針對有氧動作會改為輸入時間（分鐘），不再顯示重量組數表格。
* `WorkoutSessionEditor` 的對話框改為點擊外部不會自動關閉，避免管理訓練紀錄時誤觸而遺失輸入內容；重量與次數下拉改良為可直接輸入數值並自動套用最接近選項，無需再手動點選一次。
* Home 頁面詳情面板針對有氧動作顯示時長標籤，月曆日期會加上「有氧」標示，示範資料新增跑步機慢跑紀錄。
* 訓練紀錄可勾選「教練課」，Pinia store 會保存旗標並在月曆與詳情面板以黃色標示提醒。
* 「管理訓練紀錄」對話框不再預設產生空的訓練項目，允許僅記錄飲食也能完成儲存流程。
* REST API 與持久層擴充 `nutrition` 與 `isCoachSession` 欄位，包含 MySQL schema、hydrate/save 流程與 demo service，同步支援完整資料往返。
* 建立 Vitest 測試環境（`vitest.config.ts`、`tests/setup.ts`），新增 `useResponsiveDialog` 與 `useHomeCalendar` 的單元測試範例，相關說明整理於 `test.md`。
* 訓練動作改為所有會員共用的全域資料表，API 新增 `POST /api/exercises`、`PUT /api/exercises/:id`、`DELETE /api/exercises/:id`，並在 schema 初始化時自動合併舊有重複項目，確保「名稱 + 分類」唯一。

**下一步 TODO（優先順序）：**

1. **補齊單元測試（最高優先）**：補強日曆彈窗與 session 編輯流程的單元測試，確保資料流與 UI 行為穩定。
2. 強化訓練紀錄編輯體驗（例如快速新增組數、動作搜尋、複製既有紀錄等）。
3. 擴充錯誤處理與使用者提示（例如 localStorage 空間不足或初始化失敗時的 fallback 流程）。
4. 規劃資料匯入 / 匯出或同步機制，讓使用者可備份或跨裝置轉移紀錄。
5. 加入使用者註冊 / 密碼重設流程與更完整的權限管理。
6. 允許匯出訓練動作清單與使用者資料，並導入更完整的錯誤提示流程。
